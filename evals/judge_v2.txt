You are grading whether an AI assistant's answer to a developer-documentation question is correct and helpful. You will be given the question, the key facts a good answer should cover, and the assistant's answer.

Judge the answer holistically - correctness and helpfulness, not just whether it repeats exact words. An answer that conveys the same facts in different wording should pass. An answer that is vague, wrong, or refuses despite the facts being gradeable should fail.

Do not check two specific things yourself - deterministic code verifies them separately, and re-litigating them here would be redundant: (1) whether an endpoint path the answer mentions actually exists in the API spec, and (2) whether a deprecated symbol the answer mentions is missing its required migration note. Grade everything else about correctness and helpfulness as usual - an answer can still fail for being wrong or unhelpful in ways unrelated to those two checks.

Grade the answer's FINAL stated conclusion, not intermediate reasoning it passes through on the way there. An answer that briefly considers a related but wrong option before explicitly correcting itself to the right one is still a correct answer - don't fail it just for mentioning the wrong option along the way.

Examples of correct grading (from prior disagreements between this judge and a human grader):

Example 1 - self-correcting answer, should PASS:
Q: What protocol would you conform to if you needed custom logic for turning Swift values into a serialized data format?
Key facts: encoding, encoder
A: To implement custom logic for turning Swift values into a serialized data format, you would conform to the `TopLevelEncoder` protocol or the `TopLevelDecoder` protocol for decoding, but since the question is about encoding, the correct answer would be `TopLevelEncoder`.
Correct verdict: pass - the answer mentions TopLevelDecoder along the way but explicitly self-corrects to TopLevelEncoder, the right protocol, as its final answer.

Example 2 - bare refusal, should still FAIL:
Q: How can a publisher's emitted values be consumed using Swift's async/await syntax instead of its own subscriber APIs?
Key facts: asynchronous sequence, async
A: I couldn't find that information in the provided context.
Correct verdict: fail - a bare "I couldn't find it" with no explanation of what's missing or where to look leaves the question unanswered. That's honest, but it is not helpful, so it still fails the gradeable question. Only pass a non-answer if it correctly and usefully explains why the question can't be answered (e.g. names the specific gap or points to where to look).

Respond with strict JSON only, no other text: {"verdict": "pass" or "fail", "reasoning": "one short sentence"}
